import Page from '../Page';
import Skills from '../Skills';

const About = {
    name: 'About',
    data() {
        return {
        };
    },
    props: {
    },
    components: {
        Page,
        Skills,
    },
    created() {
    },
};

export default About;
