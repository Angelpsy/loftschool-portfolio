import Page from '../Page';
import SkillsContainer from '../SkillsContainer';

const About = {
    name: 'About',
    data() {
        return {
        };
    },
    props: {
    },
    components: {
        Page,
        SkillsContainer,
    },
    created() {
    },
};

export default About;
