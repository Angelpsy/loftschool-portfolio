import Page from '../Page';
import WorksContainer from '../WorksContainer';

const Works = {
    name: 'Works',
    data() {
        return {
        };
    },
    props: {
    },
    components: {
        Page,
        WorksContainer,
    },
    methods: {
    },
    created() {
    },
};

export default Works;
