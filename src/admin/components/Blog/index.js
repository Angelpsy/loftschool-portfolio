import Page from '../Page';
import PostsContainer from '../PostsContainer';

const Blog = {
    name: 'Blog',
    data() {
        return {
        };
    },
    props: {
    },
    components: {
        Page,
        PostsContainer,
    },
    methods: {
    },
    created() {
    },
};

export default Blog;
